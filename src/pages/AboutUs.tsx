/**
 * About Us Page
 * Uses DynamicPage component to render CMS content
 */

import { DynamicPage } from "@/components/cms";

const AboutUs = () => {
  return <DynamicPage slug="about-us" />;
};

export default AboutUs;
